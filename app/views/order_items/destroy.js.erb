$('#<%= @item.id %>').remove();
$('#total_items').html("Cart | Total items: <%= @order.order_items.count %> ");
$('total_cost').html("Your Order Total: <%= number_to_currency(@order.total_price) %>");
